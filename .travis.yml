language: python

python:
  - "2.7"

before_install:
  - "curl -s https://metrilyx.github.io/bootstrap.sh | bash -s -- nonginx"

install:
  - "sudo python setup.py install"
  - "sudo make config"

after_install:
  - "sudo make start"

branches:
  only:
    - v2.4.2-dev