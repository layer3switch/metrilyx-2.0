language: python

python:
  - "2.7"

before_install:
  - "curl -s https://metrilyx.github.io/bootstrap.sh | bash -s -- nonginx"

install:
  - "python setup.py install"
  - "make config"

after_install:
  - "make start"

branches:
  only:
    - v2.4.2-dev