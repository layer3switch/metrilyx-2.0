language: python

python:
  - "2.7"

before_install:
  - "curl -s https://metrilyx.github.io/bootstrap.sh | bash"

install:
  - "pip install -e ."
  - "python setup.py install"

script:
  - "sudo make .config"